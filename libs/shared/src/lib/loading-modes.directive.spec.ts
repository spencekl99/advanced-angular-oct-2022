import { LoadingModesDirective } from './loading-modes.directive';

describe('LoadingModesDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadingModesDirective();
    expect(directive).toBeTruthy();
  });
});
